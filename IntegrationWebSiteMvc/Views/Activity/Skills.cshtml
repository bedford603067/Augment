@model BusinessObjects.WorkManagement.SkillCollection

@{
    ViewBag.Title = "Skills";
}

<h2>Activity - Skills</h2>

<p>
    @Html.ActionLink("Create New", "EditOrCreateSkill")
</p>
<table>
    <tr>
        <th>
            Description
        </th>
        <th>
            Code
        </th>
        <th>
            Source Systems
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.HiddenFor(modelItem => item.ID)
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceSystems)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditOrCreateSkill", new { code=item.Code }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
